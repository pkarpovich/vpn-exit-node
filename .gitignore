# Secrets and local environment
.env
.env.*
!.env.example

# gluetun provider config and credentials (configs, certs, keys, auth files)
vpn-files/*
!vpn-files/.gitkeep

# OS / editor cruft
.DS_Store
.AppleDouble
.LSOverride
._*
.idea/
*.swp
