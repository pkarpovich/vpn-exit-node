# Secrets and local environment
.env
.env.*
!.env.example

# gluetun runtime / provider config that may contain credentials
vpn-files/*.conf
vpn-files/*.ovpn
vpn-files/*.key

# OS / editor cruft
.DS_Store
.AppleDouble
.LSOverride
._*
.idea/
*.swp
